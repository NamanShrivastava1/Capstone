kind: Service
apiVersion: v1
metadata:
  name: auth-service
  labels:
    app: auth
spec:
  selector:
    app: app
  type: ClusterIP
  ports:
    - name: auth-http
      port: 80
      targetPort: 3000